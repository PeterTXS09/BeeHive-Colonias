import 'dart:io';
import 'package:flutter/material.dart';

class DistritoForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Builder(

      )
    );
  }
}